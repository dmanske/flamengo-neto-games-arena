
import React from "react";
import LandingPage from "./LandingPage";

const Index = () => {
  // Simplified component that just renders the landing page
  // No redirection logic or session checking to prevent navigation issues
  return <LandingPage />;
};

export default Index;
